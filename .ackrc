# Ack is awesome: https://beyondgrep.com/
##
--smart-case
# folders I usually do not want to look into
--ignore-dir=build
--ignore-dir=dist
# files I usually ignore
--ignore-file=match:/package-lock\.json/
#
# Create other .ackrc or _ackrc files in code folders:
# ack will first searching in the current directory,
# then the parent directory, then the grandparent directory, etc.
